---
to: "<%= createPodScene ? `${cwd}/src/scenes/${podName}.scene.tsx` : null%>"
---
import React from 'react';
import { Container } from '<%=h.context().alias.common%>/components';
import { <%=h.changeCase.pascal(podName)%>Container } from '<%=h.context().alias.pods%>/<%=podName%>';

export const <%=h.changeCase.pascal(podName)%>Scene = () => (
    <Container>
        <%-`<${h.changeCase.pascal(podName)}Container />`%>
    </Container>
);