---
to: <%=cwd%>/src/pods/<%=podName%>/<%=podName%>.container.tsx
---
import React from 'react';
import { useHistory, generatePath } from 'react-router-dom';
import { switchRoutes } from '@/core/router';
import { useTheme } from '@/core/theme';
import {  } from '@/common/components';
import {  } from './<%=podName%>.api';
import {  } from './<%=podName%>.vm';
import { <%=h.changeCase.pascal(podName)%>Component } from './<%=podName%>.component';

export const <%=h.changeCase.pascal(podName)%>Container: React.FC = () => {
    const theme = useTheme();
    const history = useHistory();

    return <%-`<${h.changeCase.pascal(podName)}Component />`%>
};